# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "ExpoMASSMonsterApp"

[setup]
script = '''
$ErrorActionPreference = 'Stop'

Write-Host "== Codex setup: ExpoMASSMonsterApp =="

if (-not (Test-Path "package.json")) {
  throw "Run Codex from the repo root (package.json not found)."
}

Write-Host ("Node: " + (node -v))
Write-Host ("NPM:  " + (npm -v))

if (-not (Test-Path "node_modules")) {
  Write-Host "Installing dependencies..."
  npm install
} else {
  Write-Host "node_modules exists, skipping npm install"
}

if (-not (Test-Path ".env") -and (Test-Path ".env.example")) {
  Copy-Item ".env.example" ".env"
  Write-Host "Created .env from .env.example (fill real values)."
}

try { npx expo --version | Out-Null; Write-Host "Expo CLI OK" } catch { Write-Warning "Expo CLI check failed (non-fatal)" }
try { npx eas --version  | Out-Null; Write-Host "EAS CLI OK"  } catch { Write-Warning "EAS CLI check failed (non-fatal)" }

Write-Host ""
Write-Host "Next:"
Write-Host "  npm run start:go"
Write-Host "  npm run start:dev-client"
Write-Host "  npm test"
'''
